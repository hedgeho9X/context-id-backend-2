# 环境变量配置文件示例
# 复制此文件为 .env 并根据实际情况修改配置

# ===========================================
# 应用配置
# ===========================================

# 应用外部访问地址（用户浏览器访问的地址）
# 开发环境: http://localhost:8080
# 生产环境: https://your-domain.com 或 http://your-server-ip:8080
APP_EXTERNAL_URL=http://localhost:8080

# Casdoor外部访问地址（用户浏览器访问的认证地址）
# 开发环境: http://localhost:8000
# 生产环境: https://auth.your-domain.com 或 http://your-server-ip:8000
APP_CASDOOR_EXTERNAL_URL=http://localhost:8000

# ===========================================
# Casdoor配置
# ===========================================

# Casdoor内部通信地址（容器间通信，通常不需要修改）
CASDOOR_ENDPOINT=http://casdoor:8000

# Casdoor外部访问地址（用于生成认证URL，已被APP_CASDOOR_EXTERNAL_URL替代）
CASDOOR_EXTERNAL_ENDPOINT=http://localhost:8000

# Casdoor应用配置
CASDOOR_CLIENT_ID=ca09f4429d05c4226155
CASDOOR_CLIENT_SECRET=71a26d426be2d0312fcd2e10a072ebaa1ce51ed0
CASDOOR_ORGANIZATION_NAME=Context-ID
CASDOOR_APPLICATION_NAME=Context-ID-DEV

# JWT公钥文件路径
CASDOOR_JWT_SECRET=/app/certs/casdoor_public_key.pem

# ===========================================
# 生产环境示例配置
# ===========================================

# 如果您的服务器IP是 192.168.1.100:
# APP_EXTERNAL_URL=http://192.168.1.100:8080
# APP_CASDOOR_EXTERNAL_URL=http://192.168.1.100:8000

# 如果您有域名 example.com:
# APP_EXTERNAL_URL=https://api.example.com
# APP_CASDOOR_EXTERNAL_URL=https://auth.example.com

# 如果您使用反向代理（如Nginx）:
# APP_EXTERNAL_URL=https://your-domain.com/api
# APP_CASDOOR_EXTERNAL_URL=https://your-domain.com/auth
